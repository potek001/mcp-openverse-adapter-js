{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": ["potek001"],
  "name": "openverse-adapter",
  "version": "1.0.0",
  "description": "MCP server for retrieving openly licensed images from Openverse",
  "runtime": "node",
  "entry": "index.js",
  "tools": [
    {
      "name": "search",
      "description": "Search for openly-licensed images on Openverse",
      "parameters": {
        "type": "object",
        "required": ["query"],
        "properties": {
          "query": {
            "type": "string",
            "description": "Search terms"
          },
          "page": {
            "type": "number",
            "description": "Page number (default: 1)"
          },
          "page_size": {
            "type": "number",
            "description": "Results per page (default: 20, max: 500)"
          },
          "license_type": {
            "type": "string",
            "description": "License type (commercial or modification)"
          }
        }
      }
    },
    {
      "name": "fetch",
      "description": "Get detailed information about a specific image",
      "parameters": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Openverse image ID (UUID format)"
          }
        }
      }
    }
  ]
}
